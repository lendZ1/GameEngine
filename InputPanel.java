import java.awt.*;
import javax.swing.*;
import java.util.concurrent.BrokenBarrierException;

public class InputPanel extends BevegendePanel {
    public InputPanel(int xpos, int ypos) {
        super(xpos, ypos);
        setBackground(Color.LIGHT_GRAY);
        setBounds(xpos, ypos, 200, 100); // Set size and position
    }
}

    
